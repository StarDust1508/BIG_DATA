# 📝 Квиз — Модуль 06 «Spark MLlib»

> 25 вопросов. Ответы внизу.

---

## Часть А. Архитектура MLlib (6)

**1.** Современный API MLlib:
- a) `pyspark.mllib` (RDD-based)
- b) `pyspark.ml` (DataFrame-based, с Pipeline)
- c) `pyspark.sklearn`
- d) `pyspark.tf`

**2.** Transformer — это:
- a) Объект с `fit()`
- b) Чистая функция: `DataFrame → DataFrame`, через `transform()`
- c) Кэш
- d) Streaming

**3.** Estimator превращается в Model:
- a) автоматически
- b) через `.fit(df)`
- c) через `.cache()`
- d) через `.transform()`

**4.** Pipeline:
- a) Только для streaming
- b) Цепочка Transformer'ов и Estimator'ов, сама — Estimator
- c) Альтернатива SparkSession
- d) Запрещён в продакшене

**5.** Какой Transformer обязательно нужен перед любой моделью MLlib?
- a) `StandardScaler`
- b) `VectorAssembler` (чтобы создать колонку features)
- c) `Imputer`
- d) `OneHotEncoder`

**6.** Когда MLlib НЕ оптимален:
- a) > 100 ГБ распределённые данные
- b) < 5 ГБ, на ноутбуке — scikit-learn лучше
- c) ML встроен в Spark ETL
- d) Распределённая тренировка

---

## Часть Б. Feature engineering (6)

**7.** Чтобы превратить категориальную строку в число:
- a) `Tokenizer`
- b) `StringIndexer`
- c) `Imputer`
- d) `Scaler`

**8.** После `StringIndexer` обычно идёт:
- a) `OneHotEncoder`
- b) `IDF`
- c) `Bucketizer`
- d) ничего

**9.** `withMean=True` в `StandardScaler` опасен потому что:
- a) Медленный
- b) Денсифицирует разреженные вектора → много памяти
- c) Не работает на small data
- d) Требует Java 11

**10.** `Imputer` обучается на:
- a) Random sample
- b) Train (запоминает median/mean)
- c) Никогда не обучается
- d) Test set

**11.** `QuantileDiscretizer` отличается от `Bucketizer` тем, что:
- a) Bucketizer работает только на int
- b) QD разбивает по перцентилям, Bucketizer — по фиксированным границам
- c) QD не нужен fit
- d) Они одинаковые

**12.** Какие Transformer'ы обучаются (имеют `fit`)?
- a) Только `OneHotEncoder`
- b) `StringIndexer`, `OneHotEncoder`, `StandardScaler`, `Imputer`, `CountVectorizer`, `IDF`
- c) Только `VectorAssembler`
- d) Никакие — все без fit

---

## Часть В. Модели (7)

**13.** Какая модель — «всегда первая попытка»?
- a) GBT
- b) Logistic Regression
- c) Deep Learning
- d) Naive Bayes

**14.** Random Forest НЕ нуждается в:
- a) VectorAssembler
- b) StringIndexer
- c) StandardScaler (масштабирование)
- d) labelCol

**15.** В Spark MLlib GBTClassifier поддерживает:
- a) Только бинарную классификацию
- b) Любое число классов
- c) Только regression
- d) Только streaming

**16.** Какие метрики НЕ зависят от баланса классов?
- a) Accuracy
- b) Precision
- c) ROC-AUC
- d) F1

**17.** `randomSplit` НЕ:
- a) делит данные
- b) стратифицирован
- c) детерминирован при seed
- d) lazy

**18.** Для кластеризации перед KMeans обязателен:
- a) `OneHotEncoder`
- b) `StandardScaler` (масштабирование признаков)
- c) `IDF`
- d) `Imputer`

**19.** Silhouette score:
- a) > 0 — обязательно хорошо
- b) Ближе к 1 = лучше; около 0 = нет структуры
- c) Только для регрессии
- d) Только для бинарной

---

## Часть Г. CV, save/load, объяснимость (6)

**20.** `CrossValidator` с `numFolds=5` и grid из 12 точек:
- a) 5 обучений
- b) 12 обучений
- c) 60 обучений
- d) 1 обучение

**21.** `TrainValidationSplit` — это:
- a) То же, что CV
- b) Один train/val split — быстрее CV в K раз
- c) Только для streaming
- d) Deprecated

**22.** Что попадает в `cv_model.bestModel`?
- a) Изначальная модель
- b) Обученная PipelineModel с лучшими гиперпараметрами
- c) Только параметры
- d) Stub

**23.** Чтобы использовать Spark-модель без Spark:
- a) Невозможно
- b) Через MLeap / ONNX
- c) Только через REST
- d) Только в Spark

**24.** Disparate Impact 0.55:
- a) В норме
- b) Сигнал о дискриминации — нужно расследовать
- c) Идеально
- d) Только для регрессии

**25.** MODEL CARD — это:
- a) Картинка модели
- b) Структурированная документация модели (назначение, данные, метрики, ограничения)
- c) Файл .joblib
- d) Лицензия

---

---

## ✅ Ответы

<details>
<summary>Развернуть</summary>

1. **b.** `pyspark.ml` — стандарт.
2. **b.** Transformer = чистая функция через transform().
3. **b.** `.fit(df)` → Model.
4. **b.** Pipeline = Estimator из цепочки.
5. **b.** VectorAssembler делает features-вектор — обязателен.
6. **b.** На малых данных — scikit-learn эффективнее.
7. **b.** StringIndexer.
8. **a.** OneHotEncoder.
9. **b.** withMean денсифицирует — память.
10. **b.** Imputer обучается на train (median/mean).
11. **b.** QD = перцентили, Bucketizer = границы.
12. **b.** Все перечисленные обучаются.
13. **b.** LR — стандартная первая.
14. **c.** RF не нужен scaler.
15. **a.** GBTClassifier — только бинарная.
16. **c.** ROC-AUC не зависит от баланса.
17. **b.** Не стратифицирован.
18. **b.** KMeans → обязательно scaling.
19. **b.** Silhouette: ближе к 1 — лучше.
20. **c.** 5 fold × 12 = 60 обучений.
21. **b.** TVS — один split, в K раз быстрее.
22. **b.** Обученная Pipeline с лучшими гиперпараметрами.
23. **b.** MLeap / ONNX.
24. **b.** DI < 0.8 — сигнал.
25. **b.** Структурированная документация модели.

</details>

---

## 🎯 Оценка

- **21+:** отлично, переходите к [07 Проекты](../07_проекты/).
- **16–20:** хорошо, перечитайте слабые уроки.
- **< 16:** ещё раз практику 1 и 4.

Отметьте модуль 06 в [../ХОД_ИЗУЧЕНИЯ.md](../ХОД_ИЗУЧЕНИЯ.md).
